<div class="modal fade" id="wishModal" tabindex="-1" role="dialog" aria-labelledby="wishModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-body">
                <div class="row">
                    <h3 class="modal-title text-center" id="myModalLabel"><i class="fa fa-heart-o fa-1x"></i>已加入最愛清單!</h3>
                    <br>
                    <br>
                    <div class="col-xs-6">
                        <button type="button" class="btn pattern-btn btn-block bluebg whitetxt" onclick="checkWishList();">檢視最愛清單</button>
                    </div>
                    <div class="col-xs-6">
                        <button type="button" class="btn pattern-btn2 btn-block orangebg2 whitetxt" onclick="$('#wishModal').modal('hide')">回到賣場</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    $(function () {
        function reposition() {
            var modal = $(this),
                dialog = modal.find('.modal-dialog');
            modal.css('display', 'block');

            // Dividing by two centers the modal exactly, but dividing by three 
            // or four works better for larger screens.
            dialog.css("margin-top", Math.max(0, ($(window).height() - dialog.height()) / 2));
        }
        // Reposition when a modal is shown
        $('.modal').on('show.bs.modal', reposition);
        // Reposition when the window is resized
        $(window).on('resize', function () {
            $('.modal:visible').each(reposition);
        });
    });
    var checkWishList = function () {
        location.href = '/WishList';
    }
</script>